// Demonstrates FFI: call libc clock_gettime to get the current
// wall-clock seconds, then print it.

import std/io
import std/int

extern fn time(tloc: i64): i64

fn main(): i32 io {
    let secs = time(0i64)
    io.println("seconds since epoch: " + int.to_string_i32(secs as i32))
    return 0
}
