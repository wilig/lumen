target/
Cargo.lock
*.wasm
*.cwasm
.DS_Store

# Compiled example binaries (keep only .lm source)
examples/*
!examples/*.lm

# Keep beads DB under version control? Yes — tracks issue state across sessions.
# !.beads/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
